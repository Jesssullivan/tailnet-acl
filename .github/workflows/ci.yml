name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  secrets:
    name: Secret detection
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  check:
    name: Dhall type-check + build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Dhall type-check
        run: nix develop --command dhall type --file policy.dhall > /dev/null
        env:
          XDG_CACHE_HOME: ${{ runner.temp }}/.cache

      - name: Dhall format check
        run: |
          nix develop --command bash -c '
            find . -name "*.dhall" -exec dhall format --check {} + 2>&1
          '
        env:
          XDG_CACHE_HOME: ${{ runner.temp }}/.cache

      - name: Build policy JSON
        run: nix develop --command just build
        env:
          XDG_CACHE_HOME: ${{ runner.temp }}/.cache

      - name: Structural validation
        run: |
          nix develop --command python3 -c "
          import json, sys
          p = json.load(open('generated/policy.json'))
          required = {'groups','tagOwners','acls','grants','ssh','nodeAttrs','autoApprovers','hosts'}
          missing = required - set(p.keys())
          if missing:
              print(f'Missing keys: {missing}', file=sys.stderr)
              sys.exit(1)
          for i, r in enumerate(p['acls']):
              for f in ('action','src','dst'):
                  assert f in r, f'ACL rule {i} missing {f}'
          for i, r in enumerate(p['ssh']):
              for f in ('action','src','dst','users'):
                  assert f in r, f'SSH rule {i} missing {f}'
          for i, g in enumerate(p['grants']):
              assert 'src' in g, f'Grant {i} missing src'
              assert 'dst' in g, f'Grant {i} missing dst'
          print(f'Structural validation passed:')
          print(f'  {len(p[\"acls\"])} ACL rules')
          print(f'  {len(p[\"ssh\"])} SSH rules')
          print(f'  {len(p[\"grants\"])} grants')
          print(f'  {len(p[\"groups\"])} groups')
          print(f'  {len(p[\"tagOwners\"])} tag owners')
          "

      - name: Policy contract tests
        run: python3 -m unittest discover -s tests -p 'test_*.py'

      - name: Upload policy artifact
        uses: actions/upload-artifact@v4
        with:
          name: policy-json
          path: generated/policy.json
          retention-days: 30

  validate:
    name: Validate against live ACL
    runs-on: ubuntu-latest
    needs: [check, secrets]
    if: github.event_name == 'pull_request'
    # Deliberately the same environment the CD deploy job uses, so this job
    # reads the *same* TAILSCALE_API_KEY and TS_OAUTH_CLIENT_ID values the push
    # will read. Without it, a green run here says nothing about the deploy
    # path. See docs/ci-credentials.md.
    environment: production
    env:
      TAILSCALE_API_KEY: ${{ secrets.TAILSCALE_API_KEY }}
      TS_OAUTH_CLIENT_ID: ${{ vars.TS_OAUTH_CLIENT_ID }}
    steps:
      - uses: actions/checkout@v4

      # The runner context is unavailable in job-level env, so export the Nix
      # cache dir once here instead of repeating it on every step.
      - name: Configure cache dir
        run: echo "XDG_CACHE_HOME=$RUNNER_TEMP/.cache" >> "$GITHUB_ENV"

      # Runs before the Nix install so a dead credential fails in seconds with
      # a message naming the secret, the scope, and the fix.
      - name: Preflight Tailscale credential wiring
        run: python3 scripts/ci_preflight.py

      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Build policy
        run: nix develop --command just build

      # Server-side grammar check. push.py --dry-run only diffs locally; this
      # asks Tailscale whether the policy would be accepted at all.
      - name: Server-side policy validation
        run: python3 scripts/acl_validate.py --prove

      - name: Validate against live
        id: validate
        run: |
          set +e
          OUTPUT=$(nix develop --command python3 scripts/validate.py 2>&1)
          EXIT_CODE=$?
          echo "$OUTPUT"
          # Save for PR comment
          {
            echo 'validation_output<<EOF'
            echo "$OUTPUT"
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          if [ "$EXIT_CODE" -eq 0 ]; then
            exit 0
          fi

          if printf '%s\n' "$OUTPUT" | grep -q 'Validation FAILED: local policy differs from live.'; then
            echo "::warning::Local policy differs from live; this PR check is advisory. The CD workflow applies the policy after merge to main."
            exit 0
          fi

          exit "$EXIT_CODE"

      - name: Diff summary
        if: always()
        id: diff
        run: |
          set +e
          OUTPUT=$(nix develop --command python3 scripts/push.py --dry-run 2>&1)
          {
            echo 'diff_output<<EOF'
            echo "$OUTPUT"
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Comment on PR
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const validate = `${{ steps.validate.outputs.validation_output }}`;
            const diff = `${{ steps.diff.outputs.diff_output }}`;
            const exitCode = '${{ steps.validate.outputs.exit_code }}';
            const icon = exitCode === '0' ? ':white_check_mark:' : ':warning:';

            const body = [
              `## ${icon} ACL Validation`,
              '',
              '### Validation',
              '```',
              validate,
              '```',
              '',
              diff ? '### Diff against live' : '',
              diff ? '```' : '',
              diff || '',
              diff ? '```' : '',
            ].filter(Boolean).join('\n');

            // Find existing comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const existing = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes('ACL Validation')
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
